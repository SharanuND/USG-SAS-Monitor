import React from 'react';
import ReactDOM from 'react-dom';
import './index.module.scss'; // Import the global styles
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
